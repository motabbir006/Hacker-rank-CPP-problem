#include <iostream>
#include <iomanip> 
using namespace std;

int main() {
    int x;
    long y;
    char z;
    float p;
    double q;

    cin >> x >> y >> z >> p >> q;
    cout << x << endl << y << endl << z << endl;
    cout << fixed << setprecision(3) << p << endl; 
    cout << fixed << setprecision(9) << q << endl; 

    return 0;
}
